# Correct PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:8000/taskdb
spring.datasource.username=postgres
spring.datasource.password=pich0608
spring.datasource.driver-class-name=org.postgresql.Driver

# MyBatis
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=sreypich.rom.taskmanager.model

# Enable SQL logging (helpful for debugging)
logging.level.org.mybatis=DEBUG